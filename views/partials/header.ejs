<header class="topbar">
  <div class="crumbs">
    <h1>Mano VILNIUS TECH</h1>
    <p><%= new Date().toISOString().split('T')[0] %> • Week 2 • Lecture 6</p>
  </div>

  <div class="top-actions">
    <button class="icon-btn" type="button" aria-label="Search">
      <span aria-hidden="true">⌕</span>
    </button>
    <button class="icon-btn" type="button" aria-label="Messages">
      <span aria-hidden="true">✉</span>
    </button>
    <button class="icon-btn" type="button" aria-label="Notifications">
      <span class="dot" aria-hidden="true"></span>
      <span aria-hidden="true">🔔</span>
    </button>

    <div class="profile">
      <div class="avatar" aria-hidden="true"><%= user ? user.username.charAt(0).toUpperCase() : 'U' %></div>
      <div class="profile-meta">
        <strong id="profileName"><%= user ? user.username : 'Guest' %></strong>
        <small><%= user ? user.role : 'Visitor' %></small>
      </div>
    </div>
  </div>
</header>
