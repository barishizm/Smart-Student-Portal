<!DOCTYPE html>
<html lang="<%= currentLanguage %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VILNIUS TECH | <%= t('auth.resetTitle') %></title>
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>
    <div class="login-container">
        <a href="/" class="school-logo">
            <img src="/images/Vilnius-Gediminas-Technical-University_logo-150x150-717593362.jpg" alt="Vilnius Tech Logo">
        </a>

        <div class="login-card">
            <div class="login-header">
                <h2><%= t('auth.resetTitle') %></h2>
                <p><%= t('auth.resetSubtitle') %></p>
            </div>

            <% if(error_msg && error_msg.length > 0){ %>
                <div class="error-message show" style="display:block; color: white; background: #e74c3c; padding: 10px; border-radius: 5px; margin-bottom: 15px;">
                    <%= error_msg %>
                </div>
            <% } %>

            <% if(success_msg && success_msg.length > 0){ %>
                <div class="success-message show" style="display:block; position: relative; transform: none; opacity: 1; margin-bottom: 15px;">
                    <%= success_msg %>
                </div>
            <% } %>

            <form class="login-form" action="/auth/reset-password" method="POST" novalidate>
                <input type="hidden" name="token" value="<%= token %>">

                <div class="form-group">
                    <div class="input-wrapper password-wrapper">
                        <input type="password" id="password" name="password" placeholder=" " required autocomplete="new-password">
                        <label for="password"><%= t('auth.newPassword') %></label>
                    </div>
                </div>

                <div class="form-group">
                    <div class="input-wrapper password-wrapper">
                        <input type="password" id="confirm_password" name="confirm_password" placeholder=" " required autocomplete="new-password">
                        <label for="confirm_password"><%= t('auth.confirmPassword') %></label>
                    </div>
                </div>

                <button type="submit" class="login-btn btn">
                    <span class="btn-text"><%= t('auth.updatePassword') %></span>
                </button>
            </form>

            <div class="signup-link" style="margin-top: 20px;">
                <p><%= t('auth.backTo') %> <a href="/"><%= t('auth.signInLower') %></a></p>
            </div>
        </div>

        <%- include('partials/auth-language') %>
    </div>

    <script src="/js/auth-language.js"></script>
</body>
</html>
